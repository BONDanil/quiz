<td>
  <%= category.id %>
</td>
<td>
  <%= category.name %>
</td>
<td>
  <div class="d-flex justify-content-center">
    <%= link_to "Edit", edit_category_path(category), class: "btn btn-primary me-1" %>
    <%= button_to "Destroy", category, method: :delete, data: { turbo_confirm: 'Are you sure? All related questions will remove!' }, class: "btn btn-danger" %>
  </div>
</td>
