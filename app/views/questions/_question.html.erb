<td>
  <%= question.id %>
</td>
<td>
  <%= question.text %>
</td>
<td>
  <%= question.answer %>
</td>
<td>
  <%= question.category.name %>
</td>
<td>
  <div class="d-flex">
    <%= link_to "Edit", edit_question_path(question), class: "btn btn-primary" %>
    <%= button_to "Destroy", question, method: :delete, data: { turbo_confirm: 'Are you sure?' }, class: "btn btn-danger" %>
  </div>
</td>
